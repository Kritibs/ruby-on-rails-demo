<div class="jumbotron mt-4 p-5 bg-secondary text-white rounded">
  <h1 class="display-4">
    <%= friend.first_name %> <%= friend.last_name %>
  </h1>
  <p class="lead">
    <%= friend.email %> |
    <%= friend.phone%> |
    <%= friend.twitter %>
  </p>

  <div class="d-flex gap-3">

    <%# <% if friend.user==current_user %1> %>
  <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-primary" %> 
  <%= button_to "Delete", @friend, method: :delete, class:"btn btn-danger" %>
<%# <% end %1> %>

</div>
</div>

